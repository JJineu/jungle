def solution(grid):
    graph = [[0]*3*len(grid[0])]*(3*len(grid))
    visit = [[0]*3*len(grid[0])]*(3*len(grid))
    
    # 각 문자 위치 배정 index의 *3+1
    #
    

    answer = []
    return answer
solution(["SL","LR"])