# https://www.acmicpc.net/problem/2468
# 안전영역
# 그래프/브루트포스 알고리즘/그래프탐색/너비우선탐색/깊이우선탐색

import sys

n = int(input())
heigh = [list(map(int, input().split())) for _ in range(n)]
min_area = sys.maxsize

def rain(rained):
    min_value = min(map(min,arr))
    #map(함수, )
    visited[0]




for _ in range(나온 수의 최댓값)

위아래, 왼쪽오른쪽 f 이고 현재값이 t일때만 카운트

카운트의 최댓값
